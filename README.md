# Video → Wireframe AI

Upload a product demo video and automatically generate wireframes and Figma JSON.

## Run locally

pip install -r requirements.txt
streamlit run app.py

## Deploy

Push this repo to GitHub and deploy using Streamlit Community Cloud.